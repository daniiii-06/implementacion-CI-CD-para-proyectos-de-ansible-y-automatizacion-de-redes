---
all:
  children:
    routers:
      hosts:
        R1:
          ansible_host: 192.168.122.11  # Ajusta esta IP a la de R1
        R2:
          ansible_host: 192.168.122.12  # Ajusta esta IP a la de R2
        R3:
          ansible_host: 192.168.122.13  # Ajusta esta IP a la de R3
      vars:
        ansible_user: admin
        ansible_password: ""  # Ajusta la contraseña o usa ansible-vault si es requerida
        ansible_connection: network_cli
        ansible_network_os: community.routeros.routeros
